using MBT;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DwarfBulborb : MonoBehaviour
{
    private Blackboard board;
    void Awake()
    {
        board = GetComponent<Blackboard>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
